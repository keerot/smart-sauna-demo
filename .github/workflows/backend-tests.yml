name: Backend tests

on:
  push:
    paths:
      - "smart-sauna-backend/**"
      - ".github/workflows/backend-tests.yml"
  pull_request:
    paths:
      - "smart-sauna-backend/**"
      - ".github/workflows/backend-tests.yml"

jobs:
  test:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: smart-sauna-backend

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: "smart-sauna-backend/package-lock.json"

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm test
